# gmanka nvim theme

install with lazy:

```lua
{
  'gmankab/gmanka_nvim_theme',
  priority = 1000,
  dependencies = { 'rktjmp/lush.nvim' },
  config = function()
    vim.cmd.colorscheme('gmanka')
  end,
}
```
